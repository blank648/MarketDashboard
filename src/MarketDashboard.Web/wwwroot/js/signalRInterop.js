// SignalR connection helper — used by Blazor components via IJSRuntime if needed
window.MarketDashboard = window.MarketDashboard || {};
window.MarketDashboard.signalR = {
    // Reserved for future use by chart interop (Module 10)
    // Blazor Server components use HubConnection directly via C# SignalR client
};
